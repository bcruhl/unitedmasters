const initialState = {
  tree: null, //type: TreeNode
  interactions: { // Contains all user triggered event data (search queries, clicks)
    selectedId: null //type: string
  },
}

export default initialState
